const sx = document.getElementById('sx');        // Элемент для записи значения screenXconst sy = document.getElementById('sy');        // Элемент для записи значения screenYconst px = document.getElementById('px');        // Элемент для записи значения pageXconst py = document.getElementById('py');        // Элемент для записи значения pageYconst cx = document.getElementById('cx');        // Элемент для записи значения clientXconst cy = document.getElementById('cy');        // Элемент для записи значения clientYfunction showPosition(event) {                 // Объявляем функцию  sx.value = event.screenX;                    // Обновляем элемент, используя значение screenX  sy.value = event.screenY;                    // Обновляем элемент, используя значение screenY  px.value = event.pageX;                      // Обновляем элемент, используя значение pageX  py.value = event.pageY;                      // Обновляем элемент, используя значение pageY  cx.value = event.clientX;                    // Обновляем элемент, используя значение clientX  cy.value = event.clientY;                    // Обновляем элемент, используя значение clientY}const el = document.getElementById('body');      // Получаем элемент bodyel.addEventListener('mousemove', showPosition, false); // Позиция обновляется при перемещении мыши
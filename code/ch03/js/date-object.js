// Создаем константу для хранения нового объекта Date (по умолчанию)const today = new Date();// Создаем константу для хранения текущего годаconst year = today.getFullYear();// Создаем константу el для записи элемента с идентификатором footerconst el = document.getElementById('footer');// Записываем значение года в элемент.el.innerHTML = `<p>Copyright &copy; ${year}</p>`;
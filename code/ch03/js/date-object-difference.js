// Создаем константу для хранения нового объекта Date (по умолчанию)const today = new Date();// Получаем значение текущего годаconst year = today.getFullYear();// Устанавливаем дату создания компанииconst est = new Date('Apr 16, 1996 15:45:55');// Получаем разницу между датой создания и текущей датой в миллисекундахlet difference = today.getTime() - est.getTime();// Вычисляем количество лет, преобразуя миллисекунды в годаdifference = (difference / 31556900000);// Создаем переменную elMsg для записи элемента с идентификатором messageconst elMsg = document.getElementById('message');// Записываем сообщение в элемент.elMsg.textContent = `${Math.floor(difference)} лет мы предоставляем вам услуги авиаперелетов`;
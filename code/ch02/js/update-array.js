// Создаем массив и присваиваем значенияconst colors = ['белый', 'черный', 'пользовательский'];const color_values = ['white', 'black', '#f5f5dc'];// Обновляем третий элемент в массивеcolors[2] = 'бежевый';// Получаем элемент с идентификатором colorsconst el = document.getElementById('colors');// Заменяем его третьим элементом из массиваel.textContent = colors[2];/* NOTE: textContent не работает в браузере IE8 и версии ниже
Вы можете использовать innerHTML в строке 10, но возникает угроза безопасности сайта, как описано в книге:el.innerHTML = colors[2];*/// Добавим интерактивностиlet index =0;setInterval(function() {	if(index === colors.length) {		index = 0;	}	el.textContent = colors[index];	content.style.backgroundColor = color_values[index];	index++;}, 3000);
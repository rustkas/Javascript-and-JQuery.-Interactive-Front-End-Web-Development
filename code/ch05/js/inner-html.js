const items = [	document.getElementById('one'),	document.getElementById('two'),	document.getElementById('three'),	document.getElementById('four'),	];console.log('items', items);const timeShift = 500;	for(let i = 0; i < items.length; i++){	//console.log(i,'setTimeout');	const timerValue = timeShift + (i+1)*1000;	setTimeout(function(){						// Сохраняем элемент списка в переменной		const item = items[i];		console.log('index', i, item);				// Получаем содержимое первого элемента списка		const itemContent = item.innerHTML;		// Обновляем содержимое первого элемента списка, делая из него ссылку		item.innerHTML = `<a href="http://example.org">${itemContent}</a>`;			}, timerValue);}	
const elements = document.getElementsByClassName('hot'); // Находим элементы с классом hotconsole.log('elements.length', elements.length);setTimeout(function(){	if (elements.length > 2) {             // Если найдено 3 или более		const el = elements[2];            // Выделяем третий элемент из NodeList		el.className = 'cool';             // Изменяем значение атрибута class этого элемента	}}, 2000);
// Метод querySelector() возвращает только первое совпадение.let el = document.querySelector('li.hot1');if (el === null) {	el = document.querySelector('li.hot');	}setTimeout(function(){	if(el !== null) {		el.className = 'cool';	}},1500);// Метод querySelectorAll возвращает NodeList.// Второй совпадающий с запросом элемент (третий в списке) выбирается и изменяется.let els = document.querySelectorAll('li.hot2');console.log("document.querySelectorAll('li.hot2')", els);if(els.length === 0) {	els = document.querySelectorAll('li.hot');}setTimeout(function(){	if(els.length >= 1) {		els[1].className = 'cool';	}},2000);
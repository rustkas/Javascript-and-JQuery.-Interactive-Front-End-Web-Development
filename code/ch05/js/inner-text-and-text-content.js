const firstItem = document.getElementById('one'); // Находим первый элемент спискаconst showTextContent = firstItem.textContent;    // Получаем значение textContentconst showInnerText = firstItem.innerText;        // Получаем значение innerText// Отображаем содержимое двух этих свойств после спискаvar msg =	`<p>textContent: ${showTextContent}</p>			 <p>innerText: ${showInnerText}</p>`;setTimeout(function(){	const el = document.getElementById('scriptResults');	el.innerHTML = msg;}, 1500);setTimeout(function(){	console.log('firstItem.textContent', firstItem.textContent);	console.log('firstItem.innerText', firstItem.innerText);	const tooltip = firstItem.textContent;	//firstItem.textContent = 'пшеничные сухарики';       // Обновляем первый элемент списка	firstItem.innerText = 'пшеничные сухарики'; 	firstItem.title = tooltip;	console.log('firstItem', firstItem);}, 3500);
const elements = document.getElementsByTagName('li'); // Находим элементы lilet index = 0;const timeInterval = setInterval(function(){	console.log('index', index);	if (elements.length > 0) {  		// Если найден один или более элементов		const el = elements[index];		// Выбираем первый из них при помощи синтаксиса массивов		el.className = 'cool';			// Изменяем значение класса		index++;				if(index === elements.length) {			clearTimeout(timeInterval);			setTimeout(function(){				console.log('the end');				}, 1000);					}	}	}, 1500);
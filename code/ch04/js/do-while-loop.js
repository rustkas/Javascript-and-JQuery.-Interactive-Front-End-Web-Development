let i = 1;       // Устанавливаем значение счетчика равным 1let msg = '';    // Сообщение// Сохраняем в переменной таблицу умножения на 5do {  msg += `${i} x 5 = ${(i * 5)}<br />`;  i++;} while (i < 11); // Обратите внимание: при значении 1 код продолжает работатьdocument.getElementById('answer').innerHTML = msg;